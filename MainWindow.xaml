<Window x:Class="HardwareScope.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="别离检测工具" Width="1280" Height="820" MinWidth="1040" MinHeight="680"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize"
        Background="#090E1B" FontFamily="{StaticResource AppFont}" Icon="/Assets/app-icon.ico">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" CornerRadius="0"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#11182A"/>
        <SolidColorBrush x:Key="PanelAltBrush" Color="#151E33"/>
        <SolidColorBrush x:Key="LineBrush" Color="#26324D"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#8E9AB8"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#6D83FF"/>
        <SolidColorBrush x:Key="GreenBrush" Color="#45E0B7"/>

        <Style x:Key="ScrollPageButtonStyle" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="VerticalScrollThumbStyle" TargetType="Thumb">
            <Setter Property="MinHeight" Value="42"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value><ControlTemplate TargetType="Thumb"><Border x:Name="ThumbBody" Background="#43537C" BorderBrush="#6479F3" BorderThickness="1" CornerRadius="5"/><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ThumbBody" Property="Background" Value="#6479F3"/><Setter TargetName="ThumbBody" Property="BorderBrush" Value="#8295FF"/></Trigger><Trigger Property="IsDragging" Value="True"><Setter TargetName="ThumbBody" Property="Background" Value="#45BFA6"/><Setter TargetName="ThumbBody" Property="BorderBrush" Value="#65E7C7"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="HorizontalScrollThumbStyle" TargetType="Thumb">
            <Setter Property="MinWidth" Value="42"/><Setter Property="Margin" Value="2"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Thumb"><Border x:Name="ThumbBody" Background="#43537C" BorderBrush="#6479F3" BorderThickness="1" CornerRadius="5"/><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ThumbBody" Property="Background" Value="#6479F3"/></Trigger><Trigger Property="IsDragging" Value="True"><Setter TargetName="ThumbBody" Property="Background" Value="#45BFA6"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="12"/>
            <Setter Property="Margin" Value="2,1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Border Background="#10192B" BorderBrush="#25324D" BorderThickness="1" CornerRadius="6">
                            <Track x:Name="PART_Track" Orientation="Vertical" IsDirectionReversed="True" Focusable="False">
                                <Track.DecreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{StaticResource ScrollPageButtonStyle}"/></Track.DecreaseRepeatButton>
                                <Track.Thumb><Thumb Style="{StaticResource VerticalScrollThumbStyle}"/></Track.Thumb>
                                <Track.IncreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{StaticResource ScrollPageButtonStyle}"/></Track.IncreaseRepeatButton>
                            </Track>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/><Setter Property="Height" Value="12"/><Setter Property="Margin" Value="1,2"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Border Background="#10192B" BorderBrush="#25324D" BorderThickness="1" CornerRadius="6">
                                    <Track x:Name="PART_Track" Orientation="Horizontal" IsDirectionReversed="False" Focusable="False">
                                        <Track.DecreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Style="{StaticResource ScrollPageButtonStyle}"/></Track.DecreaseRepeatButton>
                                        <Track.Thumb><Thumb Style="{StaticResource HorizontalScrollThumbStyle}"/></Track.Thumb>
                                        <Track.IncreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Style="{StaticResource ScrollPageButtonStyle}"/></Track.IncreaseRepeatButton>
                                    </Track>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StartupProgressStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="11"/><Setter Property="Background" Value="#18223A"/><Setter Property="Foreground" Value="#6D83FF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" BorderBrush="#2C3A5A" BorderThickness="1" CornerRadius="6" Padding="2">
                            <Grid x:Name="PART_Track" ClipToBounds="True">
                                <Border x:Name="PART_Indicator" HorizontalAlignment="Left" CornerRadius="4">
                                    <Border.Background><LinearGradientBrush StartPoint="0,0" EndPoint="1,0"><GradientStop Color="#6D83FF" Offset="0"/><GradientStop Color="#45E0B7" Offset="1"/></LinearGradientBrush></Border.Background>
                                </Border>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="20"/>
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#9CA7C2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="46"/>
            <Setter Property="Margin" Value="10,3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="NavBorder" Background="Transparent" CornerRadius="10" Padding="14,0">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition/></Grid.ColumnDefinitions>
                                <TextBlock Text="{TemplateBinding Tag}" FontSize="17" VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}"/>
                                <ContentPresenter Grid.Column="1" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="NavBorder" Property="Background" Value="#18213A"/></Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="NavBorder" Property="Background" Value="#202A50"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18,9"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.86"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#202A40"/>
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0D1425"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="11,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#0D1425"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
            <Setter Property="Padding" Value="11,8"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#EDF1FF"/>
            <Setter Property="Foreground" Value="#172038"/>
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#172038"/>
            <Setter Property="Padding" Value="8,5"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#D7DDF0"/>
            <Setter Property="Margin" Value="0,7"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Foreground" Value="#E8ECF7"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#202A40"/>
            <Setter Property="RowBackground" Value="#11182A"/>
            <Setter Property="AlternatingRowBackground" Value="#131B2E"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="RowHeight" Value="43"/>
            <Setter Property="ColumnHeaderHeight" Value="40"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="EnableRowVirtualization" Value="True"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#18213A"/>
            <Setter Property="Foreground" Value="#AEB8D2"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#2A3652"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#27335B"/></Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border x:Name="RootBorder" BorderBrush="#303B5A" BorderThickness="1" Background="#090E1B">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="54"/><RowDefinition Height="*"/><RowDefinition Height="30"/></Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#0E1526" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="18,0">
                    <Border Width="32" Height="32" CornerRadius="9" Background="#101A32" BorderBrush="#36486F" BorderThickness="1">
                        <Image Source="/Assets/app-icon-final.png" Width="27" Height="27" Stretch="Uniform"/>
                    </Border>
                    <TextBlock Text="别离检测工具" Foreground="White" FontSize="15" FontWeight="SemiBold" Margin="11,0,0,0" VerticalAlignment="Center"/>
                    <Border Background="#18213A" CornerRadius="8" Padding="8,3" Margin="12,0,0,0">
                        <TextBlock Text="硬件检测" Foreground="#8692AE" FontSize="11"/>
                    </Border>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="—" Width="46" Height="54" Background="Transparent" Foreground="#BCC5D9" BorderThickness="0" FontSize="16" Click="Minimize_Click"/>
                    <Button Content="□" Width="46" Height="54" Background="Transparent" Foreground="#BCC5D9" BorderThickness="0" FontSize="14" Click="Maximize_Click"/>
                    <Button Content="×" Width="46" Height="54" Background="Transparent" Foreground="#BCC5D9" BorderThickness="0" FontSize="22" Click="Close_Click"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="212"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Border Background="#0C1322" BorderBrush="#1E2940" BorderThickness="0,0,1,0">
                    <Grid>
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <TextBlock Text="工作台" Foreground="#687590" FontSize="11" FontWeight="Bold" Margin="24,22,0,10"/>
                        <StackPanel Grid.Row="1">
                            <RadioButton Content="总览" Tag="⌂" GroupName="Navigation" Style="{StaticResource NavButtonStyle}" IsChecked="True" Click="Nav_Click" CommandParameter="OverviewPage"/>
                            <RadioButton Content="全部硬件" Tag="▦" GroupName="Navigation" Style="{StaticResource NavButtonStyle}" Click="Nav_Click" CommandParameter="HardwarePage"/>
                            <RadioButton Content="温度监控" Tag="◉" GroupName="Navigation" Style="{StaticResource NavButtonStyle}" Click="Nav_Click" CommandParameter="TemperaturePage"/>
                            <RadioButton x:Name="SettingsNav" Content="详细设置" Tag="⚙" GroupName="Navigation" Style="{StaticResource NavButtonStyle}" Click="Nav_Click" CommandParameter="SettingsPage"/>
                        </StackPanel>
                        <Border Grid.Row="2" Background="#121A2B" CornerRadius="12" Padding="13" Margin="12,0,12,16">
                            <StackPanel>
                                <TextBlock Text="实时传感器" Foreground="#7F8BA8" FontSize="11"/>
                                <StackPanel Orientation="Horizontal" Margin="0,7,0,0">
                                    <Ellipse Width="8" Height="8" Fill="{StaticResource GreenBrush}" Margin="0,0,7,0"/>
                                    <TextBlock x:Name="SidebarSensorCount" Text="正在连接…" Foreground="White" FontWeight="SemiBold"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <Grid Grid.Column="1" Margin="28,22,28,18">
                    <!-- 总览 -->
                    <Grid x:Name="OverviewPage">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,24,0">
                                <TextBlock Text="电脑总览" Foreground="White" FontSize="27" FontWeight="SemiBold"/>
                                <TextBlock x:Name="MachineText" Text="正在读取这台电脑…" Foreground="{StaticResource MutedBrush}" FontSize="13" Margin="0,5,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Button x:Name="GameModeButton" Content="进入游戏模式" Style="{StaticResource PrimaryButton}" Background="#2FAF91" Margin="0,0,10,0" Click="GameMode_Click"/>
                                <Button Content="导出硬件信息" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0" Click="ExportHardware_Click"/>
                                <Button x:Name="RescanButton" Content="重新检测" Style="{StaticResource SecondaryButton}" Click="Rescan_Click"/>
                            </StackPanel>
                        </Grid>
                        <UniformGrid Grid.Row="1" Columns="4" Margin="0,22,0,18">
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                                <StackPanel><TextBlock Text="处理器" Foreground="#7F8BA8" FontWeight="Bold"/><TextBlock x:Name="CpuNameText" Text="检测中" Foreground="White" FontSize="15" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,12,0,0" MaxHeight="44"/><TextBlock Text="中央处理器" Foreground="#687590" Margin="0,8,0,0"/></StackPanel>
                            </Border>
                            <Border Style="{StaticResource CardStyle}" Margin="3,0,7,0">
                                <StackPanel><TextBlock Text="显卡" Foreground="#7F8BA8" FontWeight="Bold"/><TextBlock x:Name="GpuNameText" Text="检测中" Foreground="White" FontSize="15" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,12,0,0" MaxHeight="44"/><TextBlock Text="图形处理器" Foreground="#687590" Margin="0,8,0,0"/></StackPanel>
                            </Border>
                            <Border Style="{StaticResource CardStyle}" Margin="7,0,3,0">
                                <StackPanel><TextBlock Text="内存" Foreground="#7F8BA8" FontWeight="Bold"/><TextBlock x:Name="MemoryText" Text="检测中" Foreground="White" FontSize="15" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,12,0,0" MaxHeight="44"/><TextBlock Text="物理内存" Foreground="#687590" Margin="0,8,0,0"/></StackPanel>
                            </Border>
                            <Border Style="{StaticResource CardStyle}" Margin="10,0,0,0">
                                <StackPanel><TextBlock Text="存储" Foreground="#7F8BA8" FontWeight="Bold"/><TextBlock x:Name="StorageText" Text="检测中" Foreground="White" FontSize="15" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,12,0,0" MaxHeight="44"/><TextBlock Text="本地磁盘" Foreground="#687590" Margin="0,8,0,0"/></StackPanel>
                            </Border>
                        </UniformGrid>
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="1.35*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,9,0">
                                <Grid>
                                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
                                    <DockPanel><StackPanel><TextBlock Text="悬浮窗显示内容" Foreground="White" FontSize="17" FontWeight="SemiBold"/><TextBlock Text="与桌面温度悬浮窗中的项目同步" Foreground="#6F7C99" FontSize="11" Margin="0,4,0,0"/></StackPanel><TextBlock Text="实时 / 最低 / 最高" Foreground="#6F7C99" HorizontalAlignment="Right" VerticalAlignment="Top"/></DockPanel>
                                    <TextBlock x:Name="OverviewOverlayEmptyText" Grid.Row="1" Text="请在“温度监控”中勾选需要显示的温度项目" Foreground="#7D89A5" Margin="0,24,0,0" Visibility="Collapsed"/>
                                    <ItemsControl x:Name="OverlaySensorsList" Grid.Row="1" Margin="0,14,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,7">
                                                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="90"/><ColumnDefinition Width="90"/><ColumnDefinition Width="90"/></Grid.ColumnDefinitions>
                                                    <StackPanel><TextBlock Text="{Binding OverlayDisplayTitle}" Foreground="#E8ECF7" FontWeight="SemiBold" TextWrapping="Wrap" MaxHeight="38"/><TextBlock Text="{Binding OverlayDisplaySubtitle}" Foreground="#697693" FontSize="11" TextTrimming="CharacterEllipsis"/></StackPanel>
                                                    <TextBlock Grid.Column="1" Text="{Binding CurrentText}" Foreground="#45E0B7" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                                    <TextBlock Grid.Column="2" Text="{Binding MinimumText}" Foreground="#94A0BA" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                                    <TextBlock Grid.Column="3" Text="{Binding MaximumText}" Foreground="#FFB56B" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </Border>
                            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="9,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="检测状态" Foreground="White" FontSize="17" FontWeight="SemiBold"/>
                                    <Grid Margin="0,20,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="硬件设备" Foreground="#8E9AB8"/><TextBlock x:Name="DeviceCountText" Grid.Column="1" Text="—" Foreground="White" FontWeight="Bold"/></Grid>
                                    <Grid Margin="0,17,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="温度传感器" Foreground="#8E9AB8"/><TextBlock x:Name="SensorCountText" Grid.Column="1" Text="—" Foreground="White" FontWeight="Bold"/></Grid>
                                    <Grid Margin="0,17,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="硬件分类" Foreground="#8E9AB8"/><TextBlock x:Name="HardwareCategoryCountText" Grid.Column="1" Text="—" Foreground="White" FontWeight="Bold"/></Grid>
                                    <Border Background="#17253A" CornerRadius="10" Padding="12" Margin="0,22,0,0">
                                        <StackPanel>
                                            <TextBlock x:Name="AccessHintText" Text="正在检查处理器温度读取条件…" Foreground="#91A4C3" TextWrapping="Wrap" LineHeight="20"/>
                                            <Button x:Name="InstallSensorDriverButton" Content="安装 CPU 温度支持" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Left" Margin="0,12,0,0" Visibility="Collapsed" Click="InstallSensorDriver_Click"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Grid>

                    <!-- 全部硬件 -->
                    <Grid x:Name="HardwarePage" Visibility="Collapsed">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
                        <Grid><StackPanel><TextBlock Text="全部硬件" Foreground="White" FontSize="27" FontWeight="SemiBold"/><TextBlock Text="相同类型默认归为一类，双击分类标题或点击加号展开" Foreground="{StaticResource MutedBrush}" Margin="0,5,0,0"/></StackPanel><Button Content="导出硬件信息" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource PrimaryButton}" Click="ExportHardware_Click"/></Grid>
                        <Grid Grid.Row="1" Margin="0,20,0,14"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox x:Name="HardwareSearchBox" Width="360" HorizontalAlignment="Left" TextChanged="HardwareSearch_TextChanged"/><TextBlock x:Name="HardwareCountText" Grid.Column="1" Foreground="#8E9AB8" VerticalAlignment="Center"/></Grid>
                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="HardwareGroupsControl" Margin="0,0,10,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="12" Margin="0,0,0,10">
                                            <StackPanel>
                                                <Border Background="#18213A" CornerRadius="11" Padding="16,13" MouseLeftButtonDown="GroupHeader_MouseLeftButtonDown">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                                        <Button Grid.Column="0" Width="26" Height="26" Padding="0" Click="GroupToggle_Click">
                                                            <Button.Style>
                                                                <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                                                                    <Setter Property="Content" Value="＋"/>
                                                                    <Style.Triggers><DataTrigger Binding="{Binding IsExpanded}" Value="True"><Setter Property="Content" Value="－"/></DataTrigger></Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                        <TextBlock Grid.Column="1" Text="{Binding Category}" Foreground="White" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                                        <Border Grid.Column="2" Background="#263253" CornerRadius="8" Padding="9,4"><TextBlock Text="{Binding CountText}" Foreground="#AEB9D3"/></Border>
                                                    </Grid>
                                                </Border>
                                                <Border>
                                                    <Border.Style>
                                                        <Style TargetType="Border"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding IsExpanded}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style>
                                                    </Border.Style>
                                                    <StackPanel>
                                                        <Grid Background="#121A2B" Height="38">
                                                            <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="1.25*"/><ColumnDefinition Width="90"/><ColumnDefinition Width="1.2*"/></Grid.ColumnDefinitions>
                                                            <TextBlock Text="硬件名称" Foreground="#8490AA" Margin="16,0" VerticalAlignment="Center"/><TextBlock Grid.Column="1" Text="品牌 / 制造商" Foreground="#8490AA" Margin="10,0" VerticalAlignment="Center"/><TextBlock Grid.Column="2" Text="状态" Foreground="#8490AA" Margin="10,0" VerticalAlignment="Center"/><TextBlock Grid.Column="3" Text="详细信息" Foreground="#8490AA" Margin="10,0" VerticalAlignment="Center"/>
                                                        </Grid>
                                                        <ItemsControl>
                                                            <ItemsControl.Style>
                                                                <Style TargetType="ItemsControl">
                                                                    <Setter Property="ItemsSource" Value="{x:Null}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsExpanded}" Value="True"><Setter Property="ItemsSource" Value="{Binding Devices}"/></DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </ItemsControl.Style>
                                                            <ItemsControl.ItemTemplate><DataTemplate><Grid MinHeight="42" Background="#11182A"><Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="1.25*"/><ColumnDefinition Width="90"/><ColumnDefinition Width="1.2*"/></Grid.ColumnDefinitions><TextBlock Text="{Binding Name}" Foreground="#E5EAF6" Margin="16,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/><TextBlock Grid.Column="1" Text="{Binding Manufacturer}" Foreground="#B3BDD3" Margin="10,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/><TextBlock Grid.Column="2" Text="{Binding Status}" Foreground="#8EDFC7" Margin="10,0" VerticalAlignment="Center"/><TextBlock Grid.Column="3" Text="{Binding Detail}" Foreground="#95A1BB" Margin="10,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/><Border VerticalAlignment="Bottom" Grid.ColumnSpan="4" Height="1" Background="#202A40"/></Grid></DataTemplate></ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </StackPanel>
                                                </Border>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>

                    <!-- 温度 -->
                    <Grid x:Name="TemperaturePage" Visibility="Collapsed">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
                        <Grid><StackPanel><TextBlock Text="温度监控" Foreground="White" FontSize="27" FontWeight="SemiBold"/><TextBlock Text="最低和最高温度从本次软件启动或重置时开始统计" Foreground="{StaticResource MutedBrush}" Margin="0,5,0,0"/></StackPanel><Button Content="重置最低 / 最高" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource SecondaryButton}" Click="ResetRange_Click"/></Grid>
                        <Border Grid.Row="1" Background="#152038" BorderBrush="#2C3E62" BorderThickness="1" CornerRadius="10" Padding="13" Margin="0,18,0,14">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock x:Name="TemperatureDriverHintText" Text="勾选“桌面显示”后，该温度会出现在设置所选的桌面角落。最多显示 12 项。" Foreground="#A7B5D3" VerticalAlignment="Center" TextWrapping="Wrap"/>
                                <Button x:Name="TemperatureDriverButton" Grid.Column="1" Content="安装 CPU 温度支持" Style="{StaticResource PrimaryButton}" Margin="16,0,0,0" Visibility="Collapsed" Click="InstallSensorDriver_Click"/>
                            </Grid>
                        </Border>
                        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0" ClipToBounds="True">
                            <DataGrid x:Name="TemperatureGrid" RowHeight="72">
                                <DataGrid.Columns>
                                    <DataGridCheckBoxColumn Header="桌面显示" Binding="{Binding IsOverlay, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="88"/>
                                    <DataGridTextColumn Header="类别" Binding="{Binding Category}" Width="76" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="温度项目" Binding="{Binding SensorName}" Width="1.1*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="对应设备" Binding="{Binding HardwareName}" Width="1.35*" IsReadOnly="True"/>
                                    <DataGridTemplateColumn Header="这项温度表示什么" Width="2.1*" IsReadOnly="True">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Description}" Foreground="#AEB8D2" TextWrapping="Wrap" LineHeight="18" Margin="0,8" ToolTip="{Binding Description}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="当前" Binding="{Binding CurrentText}" Width="82" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="最低" Binding="{Binding MinimumText}" Width="82" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="最高" Binding="{Binding MaximumText}" Width="82" IsReadOnly="True"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </Grid>

                    <!-- 设置 -->
                    <Grid x:Name="SettingsPage" Visibility="Collapsed">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
                        <Grid><StackPanel><TextBlock Text="详细设置" Foreground="White" FontSize="27" FontWeight="SemiBold"/><TextBlock Text="细化温度检测、悬浮显示、游戏模式和启动方式" Foreground="{StaticResource MutedBrush}" Margin="0,5,0,0"/></StackPanel><Button Content="保存并应用" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource PrimaryButton}" Click="SaveSettings_Click"/></Grid>
                        <ScrollViewer Grid.Row="1" Margin="0,20,0,0" VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,0,10,15">
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,14">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="250"/><ColumnDefinition/></Grid.ColumnDefinitions>
                                        <StackPanel><TextBlock Text="温度检测" Foreground="White" FontSize="17" FontWeight="SemiBold"/><TextBlock Text="控制刷新频率和参与监控的硬件类型" Foreground="#77839F" TextWrapping="Wrap" Margin="0,6,28,0"/></StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="刷新间隔" Foreground="#AEB8D2"/><ComboBox x:Name="IntervalComboBox" Width="180" HorizontalAlignment="Left" Margin="0,7,0,8"><ComboBoxItem Content="1 秒" Tag="1"/><ComboBoxItem Content="2 秒" Tag="2"/><ComboBoxItem Content="3 秒" Tag="3"/><ComboBoxItem Content="5 秒" Tag="5"/><ComboBoxItem Content="10 秒" Tag="10"/></ComboBox>
                                            <WrapPanel><CheckBox x:Name="CpuCheckBox" Content="处理器" Width="120"/><CheckBox x:Name="GpuCheckBox" Content="显卡" Width="120"/><CheckBox x:Name="StorageCheckBox" Content="硬盘 / 固态硬盘" Width="160"/><CheckBox x:Name="MotherboardCheckBox" Content="主板" Width="120"/></WrapPanel>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,14">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="250"/><ColumnDefinition/></Grid.ColumnDefinitions>
                                        <StackPanel><TextBlock Text="桌面温度悬浮窗" Foreground="White" FontSize="17" FontWeight="SemiBold"/><TextBlock Text="可自由拖动、固定或自动靠边隐藏，并记住最后位置" Foreground="#77839F" TextWrapping="Wrap" Margin="0,6,28,0"/></StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <CheckBox x:Name="OverlayEnabledCheckBox" Content="启用桌面悬浮窗" Checked="OverlayPreview_Changed" Unchecked="OverlayPreview_Changed"/>
                                            <Grid Margin="0,8,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><StackPanel Margin="0,0,12,0"><TextBlock Text="悬浮方式" Foreground="#AEB8D2"/><ComboBox x:Name="OverlayModeComboBox" Margin="0,7,0,0" SelectionChanged="OverlayPreview_Changed"><ComboBoxItem Content="自由拖动"/><ComboBoxItem Content="自动靠边隐藏"/><ComboBoxItem Content="固定位置"/></ComboBox></StackPanel><StackPanel Grid.Column="1"><TextBlock Text="预设位置" Foreground="#AEB8D2"/><Grid Margin="0,7,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><ComboBox x:Name="PositionComboBox" SelectionChanged="OverlayPreview_Changed"><ComboBoxItem Content="左上角"/><ComboBoxItem Content="右上角"/><ComboBoxItem Content="左下角"/><ComboBoxItem Content="右下角"/></ComboBox><Button Grid.Column="1" Content="重置位置" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="ResetOverlayPosition_Click"/></Grid></StackPanel></Grid>
                                            <StackPanel Margin="0,17,0,0">
                                                <TextBlock Text="硬件标题显示" Foreground="#AEB8D2"/>
                                                <ComboBox x:Name="OverlayHardwareLabelModeComboBox" Width="220" HorizontalAlignment="Left" Margin="0,7,0,0" SelectionChanged="OverlayPreview_Changed">
                                                    <ComboBoxItem Content="硬件规格"/>
                                                    <ComboBoxItem Content="硬件类型"/>
                                                </ComboBox>
                                                <TextBlock Text="硬件规格显示具体型号；硬件类型显示处理器、显卡、存储等分类" Foreground="#77839F" FontSize="11" Margin="0,6,0,0" TextWrapping="Wrap"/>
                                            </StackPanel>
                                            <StackPanel Margin="0,17,0,0"><TextBlock Foreground="#AEB8D2"><Run Text="字体大小："/><Run Text="{Binding ElementName=FontSizeSlider, Path=Value, StringFormat={}{0:F0}}"/></TextBlock><Slider x:Name="FontSizeSlider" Minimum="11" Maximum="24" TickFrequency="1" IsSnapToTickEnabled="True" Margin="0,13,0,0" ValueChanged="OverlayPreview_Changed"/></StackPanel>
                                            <Grid Margin="0,18,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><StackPanel Margin="0,0,12,0"><TextBlock Foreground="#AEB8D2"><Run Text="整体透明度："/><Run Text="{Binding ElementName=OverlayOpacitySlider, Path=Value, StringFormat={}{0:P0}}"/></TextBlock><Slider x:Name="OverlayOpacitySlider" Minimum="0.45" Maximum="1" TickFrequency="0.05" Margin="0,15,0,0" ValueChanged="OverlayPreview_Changed"/></StackPanel><StackPanel Grid.Column="1"><TextBlock Foreground="#AEB8D2"><Run Text="背景浓度："/><Run Text="{Binding ElementName=BackgroundOpacitySlider, Path=Value, StringFormat={}{0:P0}}"/></TextBlock><Slider x:Name="BackgroundOpacitySlider" Minimum="0.2" Maximum="1" TickFrequency="0.05" Margin="0,15,0,0" ValueChanged="OverlayPreview_Changed"/></StackPanel></Grid>
                                            <WrapPanel Margin="0,12,0,0"><CheckBox x:Name="ShowMinCheckBox" Content="显示最低温度" Width="150" Checked="OverlayPreview_Changed" Unchecked="OverlayPreview_Changed"/><CheckBox x:Name="ShowMaxCheckBox" Content="显示最高温度" Width="150" Checked="OverlayPreview_Changed" Unchecked="OverlayPreview_Changed"/></WrapPanel>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,14">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="250"/><ColumnDefinition/></Grid.ColumnDefinitions>
                                        <StackPanel><TextBlock Text="游戏模式与快捷键" Foreground="White" FontSize="17" FontWeight="SemiBold"/><TextBlock Text="快捷键在游戏内也可使用；点击快捷键按钮后直接按下新的组合键" Foreground="#77839F" TextWrapping="Wrap" Margin="0,6,28,0"/><Button x:Name="SettingsGameModeButton" Content="立即进入游戏模式" Style="{StaticResource PrimaryButton}" Background="#2FAF91" HorizontalAlignment="Left" Margin="0,16,0,0" Click="GameMode_Click"/></StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <Grid Margin="0,0,0,9"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="190"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="进入 / 退出游戏模式" Foreground="#D9E0F2" FontWeight="SemiBold"/><TextBlock Text="隐藏主界面到右下角通知区域，并让温度悬浮窗保持置顶" Foreground="#77839F" FontSize="11" Margin="0,4,14,0" TextWrapping="Wrap"/></StackPanel><Button x:Name="GameModeHotkeyButton" Grid.Column="1" Tag="GameModeHotkey" Style="{StaticResource SecondaryButton}" Click="CaptureHotkey_Click"/></Grid>
                                            <Grid Margin="0,9"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="190"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="显示 / 隐藏悬浮窗" Foreground="#D9E0F2" FontWeight="SemiBold"/><TextBlock Text="不用退出游戏即可快速开关桌面温度" Foreground="#77839F" FontSize="11" Margin="0,4,14,0"/></StackPanel><Button x:Name="OverlayToggleHotkeyButton" Grid.Column="1" Tag="OverlayToggleHotkey" Style="{StaticResource SecondaryButton}" Click="CaptureHotkey_Click"/></Grid>
                                            <Grid Margin="0,9"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="190"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="切换悬浮窗内容" Foreground="#D9E0F2" FontWeight="SemiBold"/><TextBlock Text="在已勾选项目和不同硬件分类之间轮换" Foreground="#77839F" FontSize="11" Margin="0,4,14,0"/></StackPanel><Button x:Name="OverlayContentHotkeyButton" Grid.Column="1" Tag="OverlayContentHotkey" Style="{StaticResource SecondaryButton}" Click="CaptureHotkey_Click"/></Grid>
                                            <Button Content="恢复默认快捷键" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Left" Margin="0,8,0,0" Click="ResetHotkeys_Click"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <Border Style="{StaticResource CardStyle}">
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="250"/><ColumnDefinition/></Grid.ColumnDefinitions>
                                        <StackPanel><TextBlock Text="启动选项" Foreground="White" FontSize="17" FontWeight="SemiBold"/><TextBlock Text="控制别离检测工具是否随电脑自动启动" Foreground="#77839F" TextWrapping="Wrap" Margin="0,6,28,0"/></StackPanel>
                                        <CheckBox x:Name="StartWithWindowsCheckBox" Grid.Column="1" Content="登录 Windows 后自动启动别离检测工具" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Grid>

            <Border x:Name="StartupTemperatureOverlay" Grid.Row="1" Panel.ZIndex="80" Background="#F5090E1B">
                <Grid>
                    <Border Width="500" Background="#111A2D" BorderBrush="#34486F" BorderThickness="1" CornerRadius="18" Padding="28" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="72"/><ColumnDefinition/></Grid.ColumnDefinitions>
                            <Border Width="58" Height="58" CornerRadius="15" Background="#142443" BorderBrush="#526EEA" BorderThickness="1" VerticalAlignment="Top"><Image Source="/Assets/app-icon-final.png" Width="48" Height="48"/></Border>
                            <StackPanel Grid.Column="1" Margin="18,0,0,0">
                                <TextBlock Text="正在准备温度监控" Foreground="White" FontSize="19" FontWeight="SemiBold"/>
                                <TextBlock x:Name="StartupTemperatureStageText" Text="正在加载硬件检测组件…" Foreground="#8F9DBA" Margin="0,7,0,15"/>
                                <ProgressBar x:Name="StartupTemperatureProgress" Minimum="0" Maximum="100" Value="5" Style="{StaticResource StartupProgressStyle}"/>
                                <TextBlock x:Name="StartupTemperaturePercentText" Text="5%" Foreground="#6F82B0" HorizontalAlignment="Right" Margin="0,7,0,0" FontSize="11"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Row="2" Background="#0C1322" BorderBrush="#202A40" BorderThickness="0,1,0,0">
                <Grid Margin="15,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock x:Name="StatusText" Text="准备就绪" Foreground="#7D89A5" VerticalAlignment="Center" FontSize="11"/><TextBlock x:Name="LastUpdatedText" Grid.Column="1" Text="" Foreground="#697590" VerticalAlignment="Center" FontSize="11"/></Grid>
            </Border>
        </Grid>
    </Border>
</Window>
